# Apollo.io People Enrichment API key (https://app.apollo.io -> Settings -> API)
APOLLO_API_KEY=your_apollo_api_key_here

# Defaults are fine; override only if Apollo changes their endpoint/auth.
APOLLO_BASE_URL=https://api.apollo.io/api/v1
REQUEST_TIMEOUT_SECONDS=30
